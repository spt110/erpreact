'use strict'

let Config = {
  theme: 'pure',
  prefix: 'rct-'
}

export function requireCss(name) {
  require(`../src/less/${Config.theme}/${name}`)
}

export function setConfig(opts) {
  Object.keys(opts).forEach((key) => {
    Config[key] = opts[key]
  })
}
